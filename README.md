# rest-example-spring

The application should run after the following command line:

	mvn package

	java -jar target/rest-example-spring.jar

or

    mvn spring-boot:run

Then, open a browser and type:

    http://localhost:8080
